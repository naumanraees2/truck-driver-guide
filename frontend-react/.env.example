# (Optional) Point to your hosted Django backend API (e.g., Render, Railway, Fly.io)
# If left unset or empty, the frontend will:
# - In local development: proxy '/api' calls to http://127.0.0.1:8000
# - In production without a backend: automatically fall back to its high-accuracy client HOS engine
#
# Example:
# VITE_API_BASE_URL=https://truck-driver-guide-api.onrender.com

